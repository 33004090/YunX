# 更新日志

本文档记录 YunX 的重要变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

暂无。

## [1.2.4] - 2026-08-15

### 安全

- 恢复夸克、UC、迅雷、百度、123 云盘和 139 云盘网络请求的系统证书及主机名校验，移除信任所有证书的不安全 TLS 配置。
- 139 云盘 WebView 遇到 SSL 证书错误时不再强制继续加载。
- 关闭 Android 系统备份，降低 Cookie、Token 等认证数据被外部备份的风险。
- 认证信息导出改为密码保护的 AES-GCM 加密格式，密钥通过 PBKDF2 派生；仍可导入旧版明文备份，便于迁移。

### 修复

- 修复无法分片时回退到完整下载可能在旧临时文件后继续追加、造成文件损坏的问题。
- 完善断点续传元数据持久化，保存请求头、分片数量、计划文件大小及临时文件清理标识。
- 应用重启后，将未完成的下载任务恢复为暂停状态，避免任务长期停留在“等待”或“下载中”。
- 重新下载未知大小文件前清理旧分片并重置进度。
- 校验分片响应的 `Content-Range` 起始位置，防止服务端返回错误区间时静默生成损坏文件。
- 修复夸克转存文件清理依赖可变仓库实例的问题，使下载完成后的临时文件清理可跨进程恢复。
- 修复下载完成回调可能被空实现覆盖的问题。
- HLS 下载请求现在可随任务取消，减少暂停或取消后仍继续占用网络的问题。

### 改进

- 为夸克、UC、迅雷、百度和 139 云盘目录补充分页读取；123 云盘继续使用既有分页逻辑，降低大目录文件缺失概率。
- 增强分享链接识别：域名匹配不区分大小写，自动清除尾部标点，并支持常见的“提取码/访问码”文本形式。
- 将用户可选下载并发数上限限制为 32，并同步限制网络调度器并发，降低资源耗尽和平台风控风险。
- Room 数据库升级至版本 10，提供 9 到 10 的显式迁移，保留已有下载记录。
- 更新设置页的认证信息导入、导出流程，增加密码输入和错误提示。

### 测试与文档

- 新增分享链接解析单元测试，覆盖多个网盘、大小写域名、尾部标点和提取码场景。
- 更新 README 中的网盘支持范围、登录方式、临时文件清理及加密备份说明。

### 已知限制

- 各网盘解析依赖非公开接口及协议参数，平台更新可能导致功能失效，也可能触发账号风控。
- 下载直链通常带有有效期，暂停时间过长后可能需要重新解析链接。
- HLS 下载暂不支持 AES 加密、`EXT-X-BYTERANGE` 和分片级断点续传。
- 应用内认证数据仍存储于 Android 应用私有目录，尚未接入 SQLCipher 或 Android Keystore 字段级加密。

